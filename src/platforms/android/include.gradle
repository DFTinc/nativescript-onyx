android {
    dexOptions {
        jumboMode true
    }
    compileSdkVersion 28
    defaultConfig {
        multiDexEnabled true
        minSdkVersion 18
        targetSdkVersion 28
    }
    
}
repositories {
    mavenCentral()
    google()
    jcenter()
    maven { 
        url 'https://oss.sonatype.org/content/repositories/snapshots/' 
    }
    maven {
        url 'http://nexus.diamondfortress.com/nexus/content/repositories/releases/'
    }
    maven {
        url 'http://nexus.diamondfortress.com/nexus/content/repositories/snapshots/'
    }
}
buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        // classpath 'com.android.tools.build:gradle:4.8.0'
        // classpath 'com.google.gms:google-services:3.1.1'
        // classpath 'com.github.dcendents:android-maven-gradle-plugin:2.0'
        // classpath 'org.jfrog.buildinfo:build-info-extractor-gradle:4.0.1'
    }
}
dependencies {

    compile fileTree(dir: 'libs', include: ['*.jar','*.aar'])
    // compile(name:'onyx-nativescript-1.3.1', ext:'aar') {
    //     transitive = false // 2019: changed to false
    // }

    compile 'com.google.android.gms:play-services-gcm:11.+'
    compile 'com.android.support.constraint:constraint-layout:1.1.3'
}
